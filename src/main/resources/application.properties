#Spring application information
spring.application.name=fail-wall
server.port=8090

#Config database
spring.datasource.url=jdbc:mysql://localhost:3308/failwall
spring.datasource.username=admin
spring.datasource.password=Aibles@2022
spring.jpa.database-platform=org.hibernate.dialect.MySQL5InnoDBDialect

#Config liquibase
spring.liquibase.change-log=classpath:/db/master-changelog.xml
spring.liquibase.drop-first=true

spring.mail.username = ${USERNAME}
spring.mail.password = ${PASSWORD}
